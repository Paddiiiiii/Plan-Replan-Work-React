========================================
知识抽取系统 - 快速启动指南
========================================

⚠️ 重要提示：
   不要直接运行 python kag_extraction_frontend.py
   必须使用 streamlit run 命令！

========================================
启动方式（选择一种）:
========================================

方式1: 使用启动脚本（最简单）
----------------------------------------
Windows:
   双击运行: run_extraction_frontend.bat

Linux/Mac:
   chmod +x run_extraction_frontend.sh
   ./run_extraction_frontend.sh

方式2: 使用 streamlit 命令
----------------------------------------
打开命令行，进入此目录，然后运行:

   python -m streamlit run kag_extraction_frontend.py --server.port 9501 --server.address=0.0.0.0

   或者（如果 streamlit 在 PATH 中）:
   streamlit run kag_extraction_frontend.py --server.port 9501 --server.address=0.0.0.0

方式3: 使用完整路径
----------------------------------------
streamlit run kag/examples/MilitaryDeployment/kag_extraction_frontend.py --server.port 9501 --server.address=0.0.0.0

注意: --server.address=0.0.0.0 允许局域网访问

========================================
使用步骤:
========================================

1. 启动应用后，浏览器会自动打开（通常是 http://localhost:8501）

2. 在侧边栏点击 "🔄 初始化抽取器" 按钮

3. 在文本框中输入要抽取的文本

4. 点击 "🚀 开始抽取" 按钮

5. 查看抽取过程和结果

========================================
常见问题:
========================================

Q: 看到 "missing ScriptRunContext" 警告？
A: 这是因为直接运行了Python文件。请使用 streamlit run 命令。

Q: 如何停止应用？
A: 在命令行按 Ctrl+C

Q: 浏览器没有自动打开？
A: 手动访问 http://localhost:8501

========================================

